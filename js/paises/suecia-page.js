(function(){
  function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
  const CRITERIA=[['❤️','Experiencia global','experience',25],['📸','Fotogenia','photogenic',20],['👀','Qué ver y hacer','things',15],['🍴','Gastronomía','food',10],['🚆','Transporte','transport',10],['🛡️','Seguridad','safety',10],['💰','Calidad-precio','value',10]];
  function calc(scores){if(!scores)return null;return CRITERIA.reduce((n,c)=>n+(Number(scores[c[2]])||0)*c[3]/100,0)}
  function label(n){if(n>=9)return'Fantástico';if(n>=8)return'Muy bien';if(n>=7)return'Bien';if(n>=6)return'Correcto';return'Regular'}
  function fmt(n){return n==null?'—':n.toFixed(1).replace('.',',')}
  function cardGuide(g){const rating=calc(g.scores);const details=g.scores?`<div class="guide-rating-details">${CRITERIA.map(c=>`<div><span>${c[0]} ${esc(c[1])}<small>${c[3]}%</small></span><strong>${fmt(Number(g.scores[c[2]]))}</strong></div>`).join('')}</div>`:'';return `<article class="country-guide-card"><div class="guide-image-wrap"><img src="${esc(g.image)}" alt="${esc(g.title)}"><div class="guide-score"><small>NOTA VCC</small><strong>${fmt(rating)}</strong><span>${rating!=null?label(rating):''}</span></div></div><div class="country-guide-copy"><span class="tag">${esc(g.tag)}</span><h3>${esc(g.title).toUpperCase()}</h3><p>${esc(g.text)}</p><div class="country-meta"><span>◷ ${esc(g.days)}</span></div>${details}<p class="guide-rating-note">Valoración editorial provisional · la ajustaremos con nuestras notas definitivas.</p>${g.available&&g.href?`<a class="country-btn" href="${esc(g.href)}">VER GUÍA →</a>`:'<span class="country-soon">PRÓXIMAMENTE</span>'}</div></article>`}
  function cardItinerary(i){return `<article class="country-itinerary"><img src="${esc(i.image)}" alt="${esc(i.title)}"><div class="country-itinerary-copy"><span class="tag">${esc(i.tag)}</span><h3>${esc(i.title).toUpperCase()}</h3><p>${esc(i.text)}</p>${i.available&&i.href?`<a href="${esc(i.href)}">VER ITINERARIO →</a>`:'<span class="country-soon">PRÓXIMAMENTE</span>'}</div></article>`}
  function carousel(content,type){return `<div class="sweden-carousel-wrap"><button class="carousel-arrow prev" type="button" data-carousel-prev aria-label="Anterior">←</button><div class="sweden-carousel" data-carousel="${type}">${content}</div><button class="carousel-arrow next" type="button" data-carousel-next aria-label="Siguiente">→</button></div>`}
  function render(){const c=window.SWEDEN_CONTENT,root=document.getElementById('country-content');if(!c||!root)return;const facts=c.facts.map(x=>`<div class="country-fact"><strong>${esc(x[0])}</strong><span>${esc(x[1])}</span></div>`).join('');const guides=c.guides.map(cardGuide).join('');const itineraries=c.itineraries.map(cardItinerary).join('');const planning=c.planning.map(p=>`<div class="country-plan-card"><b>${esc(p[0])}</b><span>${esc(p[1])}</span></div>`).join('');const p=c.planner;root.innerHTML=`<section class="country-hero" style="background-image:url('${esc(c.heroImage)}')"><div class="country-hero-inner"><div class="country-breadcrumb"><a href="../../index.html">Inicio</a><span>›</span><a href="../index.html">Destinos</a><span>›</span><strong>Suecia</strong></div><div class="country-hero-card"><div class="country-eyebrow">📍 EUROPA</div><h1>SUECIA</h1><p class="country-script">${esc(c.tagline)}</p><p class="country-desc">${esc(c.description)}</p><div class="country-facts">${facts}</div></div></div></section><nav class="country-nav"><div class="country-nav-inner"><a href="#guias">GUÍAS</a><a href="#itinerarios">ITINERARIOS</a><a href="#planifica">PLANIFICA</a></div></nav><main class="country-main sweden-main"><section id="guias"><div class="country-heading"><div><p>Descubre Suecia</p><h2>GUÍAS DE SUECIA</h2></div><span>Cada destino tiene nuestra Nota VCC, calculada con 7 criterios y dando más peso a la experiencia global, la fotogenia y qué ver y hacer.</span></div>${carousel(guides,'guides')}</section><section id="itinerarios" class="sweden-itineraries-section"><div class="country-heading"><div><p>Combina las piezas</p><h2>ITINERARIOS EN SUECIA</h2></div><span>Rutas de una sola ciudad o región y combinaciones de varias guías para aprovechar mejor cada día.</span></div>${carousel(itineraries,'itineraries')}</section><section id="planifica"><div class="country-heading"><div><p>Todo antes de salir</p><h2>PLANIFICA SUECIA</h2></div><span>La parte práctica del viaje: cuándo ir, dónde dormir, cómo moverse, presupuesto, internet, pagos, reservas y preparación.</span></div><div class="country-planning sweden-planning">${planning}</div><div class="sweden-planner" data-sweden-planner><div class="planner-copy"><span>PLANIFICADOR DE RUTA</span><h3>¿QUÉ VIAJE TE ENCAJA MEJOR?</h3><p>Elige mes, días, intereses y transporte y te proponemos las guías o itinerarios que mejor encajan.</p></div><form class="planner-form"><label>¿En qué mes viajas?<select name="month"><option value="">Aún no lo sé</option>${p.months.map(x=>`<option value="${esc(x.value)}">${esc(x.label)}</option>`).join('')}</select></label><label>¿Cuántos días tienes?<select name="days">${p.days.map(x=>`<option value="${esc(x.value)}">${esc(x.label)}</option>`).join('')}</select></label><label>¿Cómo quieres moverte?<select name="transport">${p.transports.map(x=>`<option value="${esc(x.value)}">${esc(x.label)}</option>`).join('')}</select></label><fieldset><legend>¿Qué te apetece?</legend><div class="planner-checks">${p.interests.map(x=>`<label><input type="checkbox" name="interest" value="${esc(x.value)}"><span>${esc(x.label)}</span></label>`).join('')}</div></fieldset><button type="submit">ENCONTRAR MI VIAJE →</button></form><div class="planner-result" hidden aria-live="polite"></div></div></section></main>`;setupCarousels();setupPlanner(c)}
  function setupCarousels(){document.querySelectorAll('.sweden-carousel-wrap').forEach(wrap=>{const rail=wrap.querySelector('.sweden-carousel');const step=()=>Math.max(rail.clientWidth*.72,280);wrap.querySelector('[data-carousel-prev]').addEventListener('click',()=>rail.scrollBy({left:-step(),behavior:'smooth'}));wrap.querySelector('[data-carousel-next]').addEventListener('click',()=>rail.scrollBy({left:step(),behavior:'smooth'}));})}
  function setupPlanner(c){const box=document.querySelector('[data-sweden-planner]');if(!box)return;const form=box.querySelector('form'),out=box.querySelector('.planner-result');const pool=[...c.itineraries.map(x=>({...x,type:'itinerary'})),...c.guides.map(x=>({...x,type:'guide'}))];form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form),days=parseInt(fd.get('days')||'0',10),month=parseInt(fd.get('month')||'0',10),transport=fd.get('transport')||'',interests=fd.getAll('interest');const ranked=pool.map(item=>{let score=0,season='';if(days>=item.minDays&&days<=item.maxDays)score+=8;else score+=Math.max(0,4-Math.abs(days-((item.minDays+item.maxDays)/2)));if(month){if((item.bestMonths||[]).includes(month)){score+=5;season='Época ideal'}else if((item.possibleMonths||[]).includes(month)){score+=1;season='Se puede viajar'}else{score-=4;season='Menos recomendable'}}if(transport&&item.transports.includes(transport))score+=3;else if(transport)score-=1;score+=interests.filter(x=>item.interests.includes(x)).length*4;return{...item,score,season}}).sort((a,b)=>b.score-a.score);const picks=ranked.slice(0,3);out.innerHTML=`<div class="planner-result-head"><p class="country-script">Nuestra recomendación ♡</p><h3>${days} DÍAS EN SUECIA</h3></div><div class="planner-results-grid">${picks.map((x,i)=>`<article><span>${i===0?'MEJOR ENCAJE':'TAMBIÉN TE PUEDE ENCAJAR'}</span><h4>${esc(x.title)}</h4><p>${esc(x.text)}</p><small>${x.minDays===x.maxDays?x.minDays:`${x.minDays}–${x.maxDays}`} días</small>${x.season?`<em class="season-fit">${esc(x.season)}</em>`:''}<b>PRÓXIMAMENTE</b></article>`).join('')}</div>`;out.hidden=false;out.scrollIntoView({behavior:'smooth',block:'nearest'})})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render);else render();
})();